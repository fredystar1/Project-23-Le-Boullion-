const Page = () => {
  return (
    <main className="page-shell">
      <section className="hero-banner">
        <h1 className="hero-message">Events</h1>
      </section>
      <section className="section-card max-w-4xl mx-auto">
        <p>
          Join tastings, featured producer nights, and seasonal classes curated
          for every experience level.
        </p>
      </section>
    </main>
  );
};

export default Page;
